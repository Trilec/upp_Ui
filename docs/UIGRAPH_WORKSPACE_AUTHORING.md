# UiGraph workspace authoring — V7 / checkpoint 02B

The V7 HTML is the visual reference, superseding V6 and the four-preview Studio.
This document defines the authoring-file boundary. Read ACTIVE_WORK for actual
publication and validation. The HTML's illustrative C++ is NOT the production API.

## Layout and style are separate, saved together

One `.uigraph.json` file contains one named family: Base layout, Base appearance,
eight optional layout overrides and eight independently optional style overrides.
Null shape entries mean inheritance, not a hidden copy. Detach layout or style
explicitly; resetting one does not reset the other. Detached entries are complete
snapshots of that section, not fine-grained property inheritance. A clone is an
independent family. Multiple files can be opened and reused; this is not a project
repository or a multi-document database.

Base is an EDIT SCOPE, not a ninth silhouette. Selecting a shape previews its
effective composition. Inherited shape sections remain read-only until detached;
there is an explicit Base edit scope. Preview shape selection must not silently
create overrides. Copy-to-all is an explicitly confirmed replacement operation.

JSON stores layout, appearance and preview in separate sections. Preview data,
ports, camera and specimen size never appear in generated production layout.
Production C++ has separate layout factories, theme-relative style factories,
shape fallback selection, class registration and node configuration. Factory
values are shared/registered once per graph view, not constructed per node paint.
Radius is applied to node.corner_radius, the production silhouette authority.

Schema identifier: `uigraph.workspace`; version: 1; authored metrics: logical96.
Final-pixel LOD thresholds and readability floors are not DPI-scaled metrics.
V6/V7 HTML mock-up JSON is not this schema and must be rejected rather than loaded
with silent loss. Load parses/validates a candidate before replacing live state.
Unknown versions/fields, bad enums, duplicate IDs/keys and oversized nesting fail.
Maximum JSON size is 8 MiB. Static images use bounded portable premultiplied RGBA
(up to 256x256); they are resource data, never a second layout description.
Derived overview assets are regenerated by the production registry.

Save writes a temporary sibling and atomically replaces the target. Failed saves
must not clear the dirty flag or destroy the previous file. Export uses the same
validated object and generates real Ui APIs without runtime JSON parsing or an
authoring-package dependency. Re-register styles after the host changes its theme.

## Shared editing operations

Utilities/UiGraphWorkspace owns the authoring document, strict JSON, generated C++
and transaction helpers. The application owns file dialogs, undo, selection and
property-editor lifetime. UiNodeGraph owns all actual component layout/rendering.

PlaceComponent handles palette creation and existing-component moves. It validates
revision, editable scope, identity, target region, insertion anchor and capacity
before committing. Move preserves data/style/ID. A region drop inserts before its
first Fill so a new Top row is not accidentally placed after exhausted capacity.
It does not invent side-column capacity or silently redirect port-lane drops.
Unreserved-region creation must be explicit in the UI.

The palette consists of production-painted Text/Icon/Image/Progress/Fields/Tags/
Actions. Actions are PAINTED cues, not a generic live-control designer. Arbitrary
controls remain host-bound through SetNodeCtrl with explicit lifetime/minimum/LOD
activation. Ordinary rows and nodes do not allocate Ctrl trees.

## V7 interface target

One workspace replaces DesignMatrix when its replacement UI is published:
left family/shape/scope/preview-data/palette; centre threshold controls, region
and overlay diagrams plus one production preview; wide hierarchy/placement/LOD
table; right Inspector / Template / Style / C++ pages using real PropertyEditor.
PropertyEditor exposes only supported runtime settings. Template styling and
component-local styling are distinct. Placement tags derive from current settings.

Drop surfaces share one validated edit. Selection uses stable component IDs.
Diagram rectangles come from retained production geometry; authoring-only empty
region targets must be labelled. Content and Overlay remain sibling Body layers,
not the mock-up's alternative nested allocation. Port lanes are non-drop targets.

Threshold movement changes policy only. LOD jump buttons explicitly move the
camera; manual camera changes clear the jump selection. Actual active LOD remains
visible. Fit / 1:1 are explicit actions. Save/open preserves authored scope and
preview state but does not turn camera into model/template data.

## Validation boundary

02B adds UiGraphWorkspaceTests for defaults, independent shape inheritance,
cloning, transaction rejection, JSON round-trip, malformed import, code-generation
structure/escaping and atomic save. Tests are source-added, not runtime PASS until
executed. `--export=<path>` writes generated C++ for an additional compile probe.
Gary's PASS at d9754c7 applies only to 01A, not 02A/02B or replacement UI.
