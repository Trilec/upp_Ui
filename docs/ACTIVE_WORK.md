# ACTIVE WORK

This file is a crash/recovery checkpoint, not project history. Remote `main` is authoritative.
Always fetch current `main`, record HEAD, preserve concurrent work, and never force-update `main`.

## CURRENT

Latest R10A source checkpoint when written:

`01b80c8dcd8dd4686e1da2216bc5a50f77a09c74`
`UIGRAPH-R10A: stabilize legacy shape migration enum`

That SHA may already be an ancestor of newer unrelated work. Refresh before acting.

## UIGRAPH R9.3 — CLOSED / ACCEPTED

R9.3A–E are Windows Debug + Release accepted.

Key outcomes:
- software rendering policy is hybrid: direct Draw for cheap primitives, shared exact raster cache for repeatable AA/composed surfaces, bounded Painter layers only where measured useful;
- shared styled-surface cache made `current_styled` approach explicit cached-AA performance;
- Graph R9.3C reduced overview/low-zoom Node Paint by roughly 50–55% Release;
- default Graph presentation is shadowless/quiet with hierarchical dot grid, hollow ports and finer connectors;
- demo editor synchronization was gated to the visible page only;
- generated C++ remains lazy behind Code/Copy/Save and is never regenerated by pan/zoom/Fit/1:1/selection/drag/paint/mode switching;
- R9.3E removed the redundant 10k auto-fit: 10k -> Reference fell from ~8.4–9.0 s to 0.144–0.189 s and measured stages account for total time.

Do not reopen renderer work without new evidence.

Detail: `docs/22_UI_RENDER_BACKEND_ROADMAP.md`

## UIGRAPH R10A — ACTIVE / WINDOWS ACCEPTANCE PENDING

Objective: canonical node silhouettes before internal node composition work.

Authored/public direction:
- `Rectangle` owns width/height + `corner_radius`;
- square = Rectangle with equal dimensions;
- rounded rectangle = Rectangle with radius > 0;
- pill/capsule = Rectangle with radius clamped to half the smaller dimension;
- `Ellipse` owns ellipse/circle through dimensions;
- distinct silhouettes remain Diamond, Triangle, Hexagon, Cloud, Document, Database, Custom.

Implementation state:
- new authored `Rectangle` uses wire 13 so historical wire 0 can remain the old flat Rectangle;
- surviving canonical silhouettes keep their established wire ids;
- historical shape values remain only for migration/source compatibility while old fixtures/recovery source are supported;
- enum definition is now identical in every translation unit, avoiding the earlier BLITZ/ODR gate problem;
- Inspector shape choices and generated-code handoff use canonical authored concepts;
- focused package: `Utilities/UiNodeGraphCanonicalShapeTest`;
- existing Graph model-switch fixture now authors Rectangle + radius rather than RoundedRectangle.

Current source requires Windows acceptance before R10A is closed.

## R10A ACCEPTANCE

Gary only validates; architectural corrections return to supervisor.

Build CLANGx64 Debug + Release:
- `Ui`
- `Utilities/UiNodeGraphCanonicalShapeTest`
- `Utilities/UiGraphTest`
- `Utilities/UiNodeGraphModelSwitchProfileTest`
- `Utilities/UiNodeGraphPanProfileTest`
- `Utilities/UiNodeGraphScaleTest`
- `Utilities/UiNodeGraphRenderLodTest`
- `Utilities/UiNodeGraphOverviewLodTest`
- `Utilities/UiNodeGraphPresentationTest`
- `Utilities/UiNodeGraphDragDamageTest`
- `examples/UiGraphDemo`

Required checks:
- canonical-shape focused summary has zero failures;
- all existing Graph tests remain zero-failure;
- Reference demo still visibly demonstrates flat/rounded/square/pill/circle/ellipse equivalents;
- Inspector authored choices are canonical only;
- generated code uses canonical authored shape names;
- hollow ports remain aligned to silhouettes;
- Reference <-> 10k remains responsive; do not regress the R9.3E return path;
- generated-code viewport invariant remains intact.

## NEXT AFTER R10A ACCEPTANCE

R10B: separate silhouette from internal node composition.
Target policies: `Auto`, `Compact`, `HeaderBody`, `PortRows`, `Custom`.
`PortRows` uses retained ordered-port row geometry; no child Ctrl per row.

Then R11: transient edge Pulse/Flow using one Graph-owned `UiFrameTicker` and retained routes.

## DO NOT REGRESS

- unrestricted application-authored Graph world coordinates;
- dirty-region ownership;
- retained geometry/spatial indexing;
- no generated-code work in viewport/paint paths;
- no one-Ctrl-per-node/port-row architecture;
- concurrent UiTheme and UiMenu fixes currently on `main`.

## RECOVERY RULE

REFRESH -> INSPECT -> IMPLEMENT -> REVIEW -> PUBLISH -> VERIFY -> VALIDATE.
Keep changes coherent, review full diff/package membership, run `git diff --check`, and publish only fast-forward checkpoints.
