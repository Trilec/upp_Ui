# Retired authoring demo

Use `examples/UiGraphComponentStudio`, now the UiGraph Node Design Workspace.
The matrix/PresentationStudio implementation was retired at WORKSPACE-02C in
favour of one live preview, region/overlay authoring and real PropertyEditor.
Its former source remains in Git history; do not restore it as a parallel editor.
General UiGraph demos and regression tests remain active.
See `docs/UIGRAPH_NODE_WORKSPACE.md` and `docs/ACTIVE_WORK.md`.
