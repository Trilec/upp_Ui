topic "Code generation contractGenerated code is derived from the Designer model.The contract for V1 is:- preserve the runtime layout tree- preserve fixed sizing, Fit, Expand, min/max, and alignment decisions- preserve explicit theme overrides when they are enabled- do not emit stale or legacy property names unless they are used as fallback- keep the code readable enough to regenerate and inspect laterThe generated source is not where application-specific logic should live.User code should sit around it, not inside it.";
[ $$0,0#00000000000000000000000000000000:Default]
[{_} 
[s0; Code generation contractGenerated code is derived from the Designer 
model.The contract for V1 is:`- preserve the runtime layout tree`- 
preserve fixed sizing, Fit, Expand, min/max, and alignment decisions`- 
preserve explicit theme overrides when they are enabled`- do 
not emit stale or legacy property names unless they are used 
as fallback`- keep the code readable enough to regenerate and 
inspect laterThe generated source is not where application`-specific 
logic should live.User code should sit around it, not inside 
it.]]