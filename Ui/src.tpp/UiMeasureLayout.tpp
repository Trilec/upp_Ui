topic "UiMeasureLayoutUiMeasureLayout is the width-aware measurement helper for controls andcontainers that need it.Use it when:- a control can change height as its width changes- a container needs a pre-layout measurement result- the caller needs preferred, minimum, and measured sizes separated cleanlyIt is intentionally narrow. Ordinary controls should keep using GetMinSize()for their cheap fallback minimum.";
[ $$0,0#00000000000000000000000000000000:Default]
[{_} 
[s0; UiMeasureLayoutUiMeasureLayout is the width`-aware measurement 
helper for controls andcontainers that need it.Use it when:`- 
a control can change height as its width changes`- a container 
needs a pre`-layout measurement result`- the caller needs preferred, 
minimum, and measured sizes separated cleanlyIt is intentionally 
narrow. Ordinary controls should keep using GetMinSize()for their 
cheap fallback minimum.]]