UiScrollPanel

UiScrollPanel combines a surface with scrollable content.

Use it when the content can exceed the visible viewport.

Notes:

- role should be visible in the Designer and should affect the surface
- explicit fill/frame overrides should behave like final on/off states
- the content area is what scrolls; the shell chrome stays separate
