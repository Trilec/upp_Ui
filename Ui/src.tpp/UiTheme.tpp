UiTheme

UiTheme resolves semantic roles into concrete palettes and metrics.

The role resolver is the main V1 styling contract.

Use it to:

- get the default surface for a control family
- keep Minimal and Pill variants aligned with the same semantic palette
- keep explicit overrides separate from theme defaults
