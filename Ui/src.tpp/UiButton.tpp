UiButton

UiButton is the standard push button control.

Use it for primary actions, secondary actions, and semantic emphasis through
theme roles.

Important V1 behavior:

- role selects the button family look
- Accent and Alert are gentle semantic emphasis, not saturated fill modes
- explicit overrides should be used only when the UI needs to deviate
