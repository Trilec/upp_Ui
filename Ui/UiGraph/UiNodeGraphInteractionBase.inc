#include <Ui/UiGraph/UiNodeGraph.h>

// Recovery wrapper marker. The byte-for-byte retained interaction implementation
// is installed by the supervisor's final Git tree using the validated source blob.
